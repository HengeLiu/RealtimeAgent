# 阅读纸质书籍与说明书详细时序图

## 1. 文档使用说明

本功能文档的通用前提、参与模块字典和配色建议，统一见：

[时序图通用说明.md](/Users/elio/dev/llm-project/OpenAIglasses_for_Navigation/docs/total/sequence-diagram/时序图通用说明.md)

---

## 2. 总览说明

“阅读纸质书籍/药品说明书”是一个以服务器为主执行的混合任务：

1. 用户通过语音发起阅读请求
2. 眼镜端抓拍高质量图片并上传服务器
3. 服务器侧智能体调用云端全模态模型进行理解
4. 云端返回文本或流式音频
5. 服务器将结果发送给眼镜播放

该任务与“寻找物体”的主要区别：

- 图片不发手机侧做高频检测，而是直接发服务器
- 主要是一次性抓拍 + 云端理解，不是持续视觉闭环

---

## 3. 时序过程一：语音输入与任务创建

```plantuml
@startuml
title 时序过程一：语音输入与阅读任务创建
autonumber
skinparam shadowing false

actor "用户" as User #F2F2F2
participant "sensor_hub\n感知总线" as SH #DFF3E3
participant "event_detector.vad\nVAD模块" as VAD #DFF3E3
participant "event_detector\n事件感知" as ED #DFF3E3
participant "glass_gateway\n眼镜接入层" as GG #DFF3E3
participant "server_gateway\n服务器接入层" as SG #FFF4CC
participant "event_router\n事件任务分发" as ER #FFF4CC
participant "agent_center\n智能体中心" as AC #FFF4CC
participant "skills_registry\n技能注册中心" as SK #FFF4CC
participant "background_task_center\n后台任务中心" as BTC #FFF4CC

User -> SH: 说“帮我读一下这个说明书”
SH -> VAD: 提交原始音频流
VAD -> ED: 输出语音事件
ED -> GG: 发送结构化语音事件
GG -> SG: 上报语音事件
SG -> ER: 转交事件
ER -> AC: 交给智能体理解
AC -> SK: 调用阅读任务创建 Skill
SK -> BTC: 创建 read_document TaskSession
@enduml
```

---

## 4. 时序过程二：抓拍与图片上传

```plantuml
@startuml
title 时序过程二：抓拍与图片上传
autonumber
skinparam shadowing false

participant "background_task_center\n后台任务中心" as BTC #FFF4CC
participant "server_gateway\n服务器接入层" as SG #FFF4CC
participant "glass_gateway\n眼镜接入层" as GG #DFF3E3
participant "sensor_hub\n感知总线" as SH #DFF3E3
participant "executor_bus\n执行总线" as EB #DFF3E3
actor "用户" as User #F2F2F2

BTC -> SG: 下发“抓拍并上传图片”指令
SG -> GG: 转发给眼镜
GG -> SH: 注册抓拍请求\n(snapshot, high_quality)
SH --> GG: 返回抓拍图片
GG -> SG: 上传图片
SG -> EB: 可选提示“正在识别内容”
EB --> User: 播报处理中
@enduml
```

---

## 5. 时序过程三：云端阅读与结果播放

```plantuml
@startuml
title 时序过程三：云端阅读与结果播放
autonumber
skinparam shadowing false

participant "server_gateway\n服务器接入层" as SG #FFF4CC
participant "agent_center\n智能体中心" as AC #FFF4CC
participant "background_task_center\n后台任务中心" as BTC #FFF4CC
participant "state_log_store\n状态与日志存储" as LOG #FFF4CC
participant "glass_gateway\n眼镜接入层" as GG #DFF3E3
participant "executor_bus\n执行总线" as EB #DFF3E3
actor "云端多模态模型" as LLM #FFF4CC
actor "用户" as User #F2F2F2

SG -> AC: 提供抓拍图片
AC -> LLM: 发起图像理解/阅读请求
LLM --> AC: 返回文本或流式音频
AC -> BTC: 更新任务为 completed
BTC -> LOG: 记录任务结果
AC -> SG: 返回阅读结果
SG -> GG: 下发文本或音频
GG -> EB: 播放阅读结果
EB --> User: 听到说明书内容
@enduml
```

---

## 6. 分阶段说明

- 阶段一是标准语音事件链路，与所有任务一致
- 阶段二的核心是高质量单次抓拍，而不是持续相机流
- 阶段三由服务器主导调用云模型，手机侧不参与主流程

---

## 7. 关键分支

- 若图片模糊，服务器可要求眼镜重新抓拍
- 若内容过长，智能体可先摘要，再按用户追问继续细读
- 若用户要求“只读药品用法用量”，可由智能体做结构化阅读

---

## 8. 建议补充的状态枚举

- `created`
- `capturing`
- `uploading`
- `reading`
- `streaming_result`
- `completed`
- `failed`
