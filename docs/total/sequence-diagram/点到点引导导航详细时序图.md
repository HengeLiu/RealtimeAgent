# 点到点引导导航详细时序图

## 1. 文档使用说明

本功能文档的通用前提、参与模块字典和配色建议，统一见：

[时序图通用说明.md](/Users/elio/dev/llm-project/OpenAIglasses_for_Navigation/docs/total/sequence-diagram/时序图通用说明.md)

---

## 2. 总览说明

点到点引导导航是一个服务器编排、手机执行、眼镜反馈的长生命周期后台任务：

1. 用户提出目的地
2. 服务器智能体完成目的地确认
3. 服务器通过 MCP 获取导航路径
4. 创建导航任务
5. 手机侧持续执行导航组件
6. 眼镜侧持续提供 IMU、相机等感知，并执行播报

---

## 3. 时序过程一：意图确认与路线获取

```plantuml
@startuml
title 时序过程一：意图确认与路线获取
autonumber
skinparam shadowing false

actor "用户" as User #F2F2F2
participant "sensor_hub\n感知总线" as SH #DFF3E3
participant "event_detector.vad\nVAD模块" as VAD #DFF3E3
participant "event_detector\n事件感知" as ED #DFF3E3
participant "glass_gateway\n眼镜接入层" as GG #DFF3E3
participant "server_gateway\n服务器接入层" as SG #FFF4CC
participant "agent_center\n智能体中心" as AC #FFF4CC
participant "mcp_registry\nMCP接入中心" as MCP #FFF4CC
actor "地图MCP服务" as AMap #FFF4CC

User -> SH: 说“我想去桂林路地铁站”
SH -> VAD: 提交原始音频
VAD -> ED: 输出语音事件
ED -> GG: 发送语音事件
GG -> SG: 上报事件
SG -> AC: 转给智能体
AC -> AC: 识别目的地并必要时追问确认
AC -> MCP: 请求地图能力
MCP -> AMap: 查询候选目的地与路线
AMap --> MCP: 返回目的地和路线
MCP --> AC: 返回结构化路线数据
@enduml
```

---

## 4. 时序过程二：导航任务创建与启动

```plantuml
@startuml
title 时序过程二：导航任务创建与启动
autonumber
skinparam shadowing false

participant "agent_center\n智能体中心" as AC #FFF4CC
participant "skills_registry\n技能注册中心" as SK #FFF4CC
participant "background_task_center\n后台任务中心" as BTC #FFF4CC
participant "server_gateway\n服务器接入层" as SG #FFF4CC
participant "phone_gateway\n手机接入层" as PG #DDEBFF
participant "local_task_center\n本地任务中心" as LTC #DDEBFF
participant "task_components.outdoor_navigation\n导航任务组件" as NAV #DDEBFF
participant "glass_gateway\n眼镜接入层" as GG #DFF3E3
participant "executor_bus\n执行总线" as EB #DFF3E3

AC -> SK: 调用 create_hybrid_task(outdoor_navigation)
SK -> BTC: 创建导航 TaskSession
BTC -> SG: 下发启动导航给手机
SG -> PG: 转发导航路线
PG -> LTC: 创建本地导航任务
LTC -> NAV: 启动户外导航组件
BTC -> SG: 下发播报指令给眼镜
SG -> GG: 转发“开始导航”
GG -> EB: 播报导航已开始
@enduml
```

---

## 5. 时序过程三：导航运行闭环

```plantuml
@startuml
title 时序过程三：导航运行闭环
autonumber
skinparam shadowing false

participant "task_components.outdoor_navigation\n导航任务组件" as NAV #DDEBFF
participant "phone_gateway\n手机接入层" as PG #DDEBFF
participant "glass_gateway\n眼镜接入层" as GG #DFF3E3
participant "sensor_hub\n感知总线" as SH #DFF3E3
participant "executor_bus\n执行总线" as EB #DFF3E3
participant "server_gateway\n服务器接入层" as SG #FFF4CC
participant "background_task_center\n后台任务中心" as BTC #FFF4CC

NAV -> PG: 请求当前位置/方向/关键视觉状态
PG -> GG: 请求眼镜上传 IMU/相机事件
GG -> SH: 拉取 IMU/视觉采样
SH --> GG: 返回感知数据
GG --> PG: 转发感知数据
PG -> NAV: 提供多模态状态
NAV -> NAV: 结合路线与感知做下一步决策
NAV -> GG: 直接发送引导建议
GG -> EB: 播报“前方直行/向左调整”
PG -> SG: 上报导航状态
SG -> BTC: 更新任务进度
@enduml
```

---

## 6. 关键分支

- 需要重新规划路线时，由服务器再次调用地图 MCP
- 手机定位和眼镜 IMU 冲突时，以任务组件的融合策略为准
- 进入“最后十米”时，可切换到独立的近场引导任务

---

## 7. 建议补充的状态枚举

- `created`
- `route_confirming`
- `route_ready`
- `running`
- `rerouting`
- `paused`
- `completed`
- `failed`
