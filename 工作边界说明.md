# 工作边界说明

本文档定义本仓库后续开发的目录边界。最重要的规则是：SDK 只解决三端系统性问题，真实场景工程只实现产品能力。

## 两条主线

| 主线 | 目录 | 目标 | 不应该放什么 |
| --- | --- | --- | --- |
| SDK 开发框架 | [openaiglass-sdk](./openaiglass-sdk) | 提供眼镜、手机、服务器之间的绑定、通讯、日志、异常处理、大模型运行时、Tool/Task/Skill、全局上下文和设备硬件能力抽象。 | 不放盲人导航、找物体、过马路等具体业务策略。 |
| 盲人 AI 眼镜业务 | [openaiglass-for-blind](./openaiglass-for-blind) | 基于 SDK 组合真实场景能力，维护需求、阶段、验收、业务代码、端侧工程和联调数据。 | 不重新实现 SDK 协议、运行时、公共模型、日志和设备绑定底座。 |

## 如何判断文件应该放哪里

如果改动是在回答“所有基于眼镜+手机+服务器的开发者都需要什么”，放到 `openaiglass-sdk`。

典型例子：

- 通信信封、消息编解码、媒体帧格式。
- 设备注册、设备组绑定、心跳、异常码。
- `BaseTool`、`BaseTask`、`BasePhoneProcessor`、`ScenarioRunner` 等抽象。
- agent-core、backend-task-core、Skill Runtime、MCP gateway。
- SDK 安装、打包、发布、公共契约和 SDK 兼容性测试。

如果改动是在回答“盲人 AI 眼镜这个产品要做什么”，放到 `openaiglass-for-blind`。

典型例子：

- 找物体、视觉辅助、导航、过马路等业务能力。
- 盲人场景的需求文档、功能设计、阶段计划、验收记录。
- 具体 iOS App、ESP32 工程、业务场景回放数据。
- 真实设备联调脚本、业务样例入口。

## 目录说明

```text
openaiglass-sdk/
  server-python/          服务端 Python SDK 包源码
  phone-ios/              iOS 通用手机 SDK 运行时
  glass-esp32/            ESP32 通用眼镜 SDK 运行时
  docs/                   SDK 架构、协议、模型、运行时和发布文档
  tests/                  SDK 单元、集成、契约和兼容性测试
  testdata/contracts/     SDK 公共契约金样
  scripts/                SDK 打包、契约、兼容性和音频样例脚本
  config/                 SDK 服务端本地配置模板

openaiglass-for-blind/
  docs/                   盲人 AI 眼镜产品、阶段、验收和迁移文档
  host/                   盲人产品宿主，包含很薄的 server / phone / glass 装配与配置
  capabilities/           真实场景业务能力实现
  testdata/               业务场景回放数据
  scripts/                业务启动、回放和跨设备联调脚本
```

## 开发约束

1. 新增 SDK 框架能力时，先补 `openaiglass-sdk/docs` 中的协议或运行时说明，再补 `openaiglass-sdk/tests`。
2. 新增盲人场景业务能力时，优先扩展 `openaiglass-for-blind/capabilities`，不要直接改 SDK 内部实现。
3. 业务代码如果需要硬件能力、任务状态或上下文，应该通过 SDK 暴露的上下文和基类获取。
4. 只有当现有 SDK 抽象无法表达业务需求时，才把新增抽象沉淀回 `openaiglass-sdk`。
5. 文档中写“已实现”前，必须能指向代码入口、测试命令或真实联调结果。
6. 跨设备能力必须写清服务端、手机端、眼镜端启动顺序和日志观察点。

## 关于旧 example 目录的处理

早期 `example/` 的定位是“SDK 官方参考案例”，所以里面曾经同时出现 `server/`、`phone/`、`glass/` 和 `capabilities/`。在当前目录拆分后，`openaiglass-for-blind` 本身已经承担真实业务工程职责，再保留 `openaiglass-for-blind/example` 会让“真实业务工程”和“业务示例工程”两层含义重叠。

因此当前不再保留 `openaiglass-for-blind/example` 作为正式目录：

1. 服务端、手机端、眼镜端产品宿主放入 `openaiglass-for-blind/host`。
2. 业务能力放入 `openaiglass-for-blind/capabilities`。
3. 离线回放和兼容性数据统一放入 `openaiglass-for-blind/testdata`。
4. 给业务开发者看的 SDK 使用说明放在 `openaiglass-for-blind/SDK安装与能力开发指南.md`。

## 三端 SDK 与业务宿主的边界

1. `openaiglass-sdk/server-python` 放服务端 Python SDK 和可发布的 `openaiglasses-sdk` 包。
2. `openaiglass-sdk/phone-ios` 放 iOS 通用手机 SDK 运行时。它可以被后续整理成 Swift Package 或 XCFramework，但不放盲人业务能力。
3. `openaiglass-sdk/glass-esp32` 放 ESP32 通用眼镜 SDK 运行时。它可以被后续整理成 ESP-IDF component，但不放盲人业务策略。
4. `openaiglass-for-blind/host` 只做盲人产品宿主装配、配置和启动说明，不承载通用 SDK 运行时主体。
5. `openaiglass-for-blind/capabilities` 放找物、导航等真实业务能力。
