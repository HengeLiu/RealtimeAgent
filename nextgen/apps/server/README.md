# server

## 目录说明

本目录存放服务器端运行时骨架代码。

## 子目录说明

- `runtime/`：服务器运行时入口
- `gateway/`：服务器接入层
- `runtime/event_router.py`：事件任务分发骨架
- `agent/`：智能体中心
- `task_center/`：后台任务中心
- `skills/`：技能注册与内置技能
- `mcp/`：MCP 接入中心
- `storage/`：状态与日志存储
- `tests/`：服务器端相关测试
