[project]
name = "openai-skills-demo"
version = "0.1.0"
description = "A minimal Python demo for implementing a Skills runtime on top of the OpenAI Python SDK."
requires-python = ">=3.10"
dependencies = [
  "openai>=1.0.0",
]

