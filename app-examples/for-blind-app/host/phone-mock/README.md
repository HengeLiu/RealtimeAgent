# host/phone-mock 样板

phone mock 用于验证视觉任务、端侧事件订阅和 `sensor.rgb` stream。可复制：

- `device-examples/python-phone/phone.mock.yaml`

后续 `old-sdk-parity-phone` lane 会把 find_object / traffic_light 的视觉任务成功路径接入 phone mock 回放。
